[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "madlight"
version = "0.2.0"
description = "Mad Light — local talk-over Meeting Atmosphere Dial"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Parth Shah" }]
keywords = ["meeting", "audio", "pipewire", "pulseaudio", "tray"]
classifiers = [
    "Environment :: X11 Applications",
    "Intended Audience :: End Users/Desktop",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Topic :: Multimedia :: Sound/Audio :: Analysis",
]
dependencies = [
    "numpy>=1.26",
    "pillow>=10.0",
    "pystray>=0.19",
    "soundcard>=0.4.3",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
madlight = "madlight.app:main"
madlite = "madlight.app:main"

[project.urls]
Homepage = "https://github.com/parth4/mad-lite"

[tool.hatch.build.targets.wheel]
packages = ["madlight", "madlite"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
